'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
/**
 * @file 浮层公共类
 *
 * - 提供宽度/位置响应计算
 */

exports.default = {
    data: function data() {
        return {
            width: '',
            height: '',
            left: '',
            top: ''
        };
    },

    computed: {
        style: function style() {
            var css = {};
            var width = this.width,
                height = this.height,
                left = this.left,
                top = this.top;

            var props = { width: width, height: height, left: left, top: top };
            for (var p in props) {
                if (props.hasOwnProperty(p)) {
                    css[p] = props[p] > 0 ? props[p] + 'px' : props[p];
                }
            }
            return css;
        }
    }
};